# 2026.06.09 VER1
1. 运行监控：修改布局，步骤时间线作为左边列，右边列放一个tabcontrol（观测结果和审计摘要作为一个page，日志作为一个page）
2. 运行监控：步骤时间线运行完需要有运行完的时间，开始运行需要提示，并且清空已经运行的note
3. 右侧整体面板：整体不要叫上下文叫工具栏，AI助手部分显示信息应该是接入的模型名称、状态，
4. 模板库：目前回滚版本的功能是怎么样的，是否支持，如果要做版本管理，至少选中的版本需要能看到时间线，类似vscode里面的git的graph。 版本管理需要有简单的更新、增加、删除、查找的功能
5. 工作流设计中，roi绑定和输出项到底是干嘛的，为啥现在我的两个测试工作流的绑定值都是一样的。且这两个表看不清最后的删除按钮的文字，需要优化表格布局
6. 设备接入与校准中，同样看不清锚点（类型、动作、识别、删除的文字），下方类型识别与说明、识别方式的可视化同样要优化，现在没有显示全部的信息。
7. 设备接入与校准，通过识别锚点坐标，会导致当软件在分辨率发生改变的时候会触发失败。我想到两个方法，一个是通过每次固定这个软件的长宽比例，然后才能进行触发点击，另一个是通过识别对应锚点的像素块进行反馈确定。评估这两个方法的可行性，进行优化。
8. 目前的两个工作流（workspace\workflows），都没有完成动作识别的闭环。请基于你的设计框架对工作流进一步理解和完善（比如什么时候要ocr，什么时候persence，什么时候template，什么时候color），完成对应执行的闭环。
9. 工作流设计：目前对于AI 自驱动工作流的编排做的还是过于简陋，参考docs\refer\IvoryOS-main、docs\refer\ivoryOS-MCP-main，吸取其优秀的设计经验，参考其代码，进一步优化整个功能。

# 2026.06.09 VER2
1. 工作流两个列表需要加标识label，提示这两个行是干嘛的，
2. 写PROMPT、AI推理的时候，看不清之前设备接入与校准已经生成了什么信息，这样用户就比较不好写prompt，应该把这部分的信息也可视化给用户，思考更好的方案，
3. 整个工作流编排，表格是好方便确认和编辑的，但是需要再加一个page，方便去看整个可视化的工作流过程

# 2026.06.09 VER3
1. 设备接入与校准中，锚点与确认需要有删除行的按钮，需要有删除已校准仪器的功能（如右击，弹出菜单，确认后删除）
2. 工作流中，已有工作流默认应该展开，同样需要有删除已有工作流的功能（如右击，弹出菜单，确认后删除）。审阅中的已读设备上下文模块删掉。
3. 确认一下，所有的动作（如点击、快捷键、回车、输入等都是调用真实接口），目前所有反馈都是调用真实的接口（如ocr可以识别、模板可以比对、颜色可以比对），如果不是请补上。
4. 反馈（ocr、模板、颜色等）没有一个用户友好的引导和交互方式，请在界面中嵌入如何使用的提示。
5. 目前llm辅助编排会出现问题（目前的等待应该是按秒来的，但是llm编排会按照毫秒来生成）
6. llm编排应该同样有个知识库、memory、skill，去沉淀各类软件编排的功能。请对这个进行设计。（这边指的是deepseek辅助设计实验流程）# SmartAccess VER3 实施方案