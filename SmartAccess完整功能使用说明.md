# SmartAccess 完整功能使用说明

> 适用项目：`E:\github_project\SmartAccess`
>
> 适用平台：Windows
>
> 文档目标：说明 SmartAccess 当前桌面端的设备接入、锚点校准、工作流编排、运行监控、系统设置、模板管理与数据采集完整使用流程。

## 1. 产品定位

SmartAccess 是面向 Windows 科学仪器上位机软件的非侵入式接入与自动化执行工具。它不要求修改原始仪器软件，也不依赖仪器厂商提供 SDK/API，而是通过窗口识别、截图校准、锚点定位、OCR、图像相似度和 Windows UI 自动化，将人工操作沉淀为可重复执行、可审计的标准工作流。

当前主要能力包括：

- 扫描 Windows 桌面窗口并绑定目标仪器软件。
- 截取目标窗口画面，管理不同业务状态下的视图。
- 在截图上框选按钮、输入框、状态区域等锚点。
- 在动作执行前通过图像一致、文字一致或组合方式确认界面状态。
- 编排单击、输入、快捷键、回车、OCR 识别和等待动作。
- 在关键动作执行前暂停流程并请求人工确认。
- 通过 OCR 动作等待目标文字出现，控制流程继续执行。
- 记录工作流运行会话、步骤日志、执行前校验结果和运行产物。
- 使用 AI 多模态模型辅助生成设备锚点配置。
- 使用 AI 文字模型辅助生成工作流草稿。
- 管理本地模板、平台模板和执行端状态。
- 监听本地文件或目录，并将设备数据可靠上传至 SmartDataHub。
- 通过系统设置页面维护工作区、OCR、AI、平台与网络配置。

推荐使用闭环：

```text
系统设置
  -> 设备接入与校准
  -> 工作流设计
  -> 运行监控
  -> 查看日志并调整配置
  -> 模板发布或数据采集
```

## 2. 启动方式与工作区

### 2.1 发布目录启动

典型发布目录为：

```text
E:\github_project\SmartAccess\dist\SmartAccess
```

主要文件和目录：

- `run_desktop.exe`：SmartAccess 桌面端启动程序。
- `.env`：项目或发布包提供的初始默认配置。
- `resource/`：图标等资源文件。
- `workspace/`：默认运行工作区。
- 其他 `.dll`、`.pyd` 和 Python 运行依赖：打包后的程序依赖，不建议手动修改。

启动命令：

```powershell
cd E:\github_project\SmartAccess\dist\SmartAccess
.\run_desktop.exe
```

如果配置了有效的 SpecLabOS 平台地址，程序启动时可能进入平台登录和执行端注册流程；未配置平台地址时，可直接使用本地功能。

### 2.2 工作区的作用

工作区是 SmartAccess 的运行数据根目录，用于保存：

- 设备与锚点配置。
- 工作流草稿。
- 本地模板。
- 工作流运行记录和截图产物。
- 数据采集配置、队列和失败记录。
- 用户级系统设置。

可在 `系统设置 -> 基础设置 -> 工作区` 中点击 `选择目录` 更换工作区。保存后，程序会：

1. 更新项目 `.env` 中的 `SMARTACCESS_WORKSPACE_DIR`。
2. 将用户级配置写入新工作区。
3. 后续从新工作区加载设备、工作流、运行记录和其他数据。

切换工作区前，应确认新目录具有读写权限。工作区改变后，原工作区中的设备、工作流和历史运行记录不会自动复制到新目录。

### 2.3 配置优先级

SmartAccess 按以下顺序解析配置，左侧优先级最高：

```text
操作系统环境变量 > 用户配置 JSON > .env > 代码内置默认值
```

用户配置文件保存于：

```text
workspace\app_state\application_settings.json
```

说明：

- `.env` 适合作为初始默认配置和部署模板。
- 系统设置页保存的内容属于当前工作区的用户级覆盖配置。
- 操作系统环境变量适合部署环境强制覆盖，系统设置页无法覆盖同名环境变量。
- 密钥输入框会以密码形式显示，文档和截图中不要公开真实密钥。

## 3. 推荐的首次使用流程

首次接入设备建议按以下顺序操作：

1. 在 `系统设置` 中确认工作区、自动化 Provider、OCR 和 AI 配置。
2. 打开目标仪器软件，并把窗口切换到准备校准的页面。
3. 在 `设备接入与校准` 中新建设备、扫描窗口并刷新截图。
4. 按业务状态创建视图，在截图上绘制锚点和执行前校验区域。
5. 使用 `校验预览` 检查图像或文字校验结果。
6. 保存设备配置。
7. 在 `工作流设计` 中创建工作流并添加动作。
8. 双击 `参数摘要` 配置动作专属参数。
9. 根据需要勾选 `执行前确认`，并设置 `动作后等待`。
10. 检查并保存工作流。
11. 在 `运行监控` 中选择工作流并启动。
12. 查看步骤日志、执行前校验详情和人工确认弹窗。
13. 根据运行结果回到设备或工作流页面进行调整。

建议先使用简单流程验证：

```text
单击按钮 -> 输入固定文本 -> 回车 -> OCR 等待结果文字
```

## 4. 主界面导航

SmartAccess 左侧导航当前包括以下页面：

1. `设备接入与校准`：窗口扫描、截图、视图、锚点和执行前校验配置。
2. `工作流设计`：动作编排、参数配置、人工确认和 AI 工作流生成。
3. `运行监控`：启动或停止工作流，处理人工确认，查看运行日志。
4. `模板/平台`：管理本地模板、平台模板和发布状态。
5. `运行概览`：查看当前工作区的设备、工作流、模板和最近运行情况。
6. `数据采集`：监听本地设备数据并上传至 SmartDataHub。
7. `系统设置`：维护工作区、默认参数、OCR、AI、平台和网络配置。

界面右侧为系统状态区域，可用于查看当前运行能力、Provider 和页面上下文信息。顶部按钮可折叠左侧导航或右侧状态面板。

## 5. 系统设置

系统设置用于维护当前工作区的用户级配置。页面顶部提供：

- `打开配置目录`：打开 `workspace\app_state`。
- `恢复 .env 默认`：清除用户级覆盖，重新使用环境变量、`.env` 和代码默认值。
- `保存配置`：保存当前设置。

页面提示中的生效规则应重点关注：

- 动作等待、OCR 默认值和图像相似度阈值可立即更新当前运行时。
- OCR、AI、平台、RabbitMQ、Edge API、UDP 等服务连接类配置通常需要重启 SmartAccess 后完整生效。
- 如果操作系统环境变量设置了同名配置，保存用户设置后仍会以环境变量为准。

### 5.1 基础设置

| 参数 | 作用 |
| --- | --- |
| 工作区 | 设备、工作流、运行产物和用户设置的根目录，通过目录选择器修改。 |
| 日志级别 | 控制程序日志详细程度，可选 `DEBUG`、`INFO`、`WARNING`、`ERROR`。 |
| 自动化 Provider | `real` 使用真实 Windows 自动化，`stub` 用于无真实操作的模拟环境。 |
| 执行端设备 ID | 当前 SmartAccess 执行端在平台和数据采集中的设备标识。 |

### 5.2 动作与校验默认值

| 参数 | 当前默认值 | 作用 |
| --- | ---: | --- |
| 动作后等待 | `1.0 秒` | 新建动作成功后额外等待的默认时间。 |
| OCR 识别超时 | `10.0 秒` | 新建 OCR 动作等待目标结果的默认最长时间。 |
| OCR 轮询间隔 | `0.5 秒` | OCR 动作两次识别之间的默认等待时间。 |
| 执行前图像相似度 | `0.8` | 新建或重新保存锚点时使用的默认图像一致阈值。 |

默认值说明：

- 修改默认值不会强制覆盖所有已保存工作流中的步骤参数。
- 新建工作流步骤和 AI 生成的工作流会使用当前运行时默认值。
- 图像相似度阈值修改后，新建锚点使用新阈值。
- 已有设备配置重新点击保存时，设备所有视图中的执行前图像校验阈值会同步更新为当前系统默认值。
- 未重新保存的设备配置继续保留原阈值。
- 动作后等待设置为 `0` 表示不增加步骤级额外等待；底层窗口激活、输入等自动化操作仍可能包含必要的短暂内部等待。

### 5.3 OCR 服务

| 参数 | 作用 |
| --- | --- |
| OCR 模式 | 选择当前使用的 OCR Provider，例如本地或 API 模式。 |
| OCR API 地址 | API OCR 模式下的服务地址。 |

OCR 是文字识别能力，主要用于：

- 执行前的文字一致校验。
- 工作流中的 `OCR识别` 动作。
- 设备校准页面的文字校验预览。

OCR 与 AI 多模态模型是两套独立能力。配置了多模态模型并不代表 OCR Provider 已配置，系统状态也会分别显示 OCR、AI 文字和 AI 多模态能力。

### 5.4 AI 服务

AI 服务分为两组：

#### 文字模型（工作流生成）

| 参数 | 作用 |
| --- | --- |
| Provider | 文字模型提供者。 |
| Base URL | 兼容接口的基础地址。 |
| 模型 | 用于生成工作流的模型名称。 |
| API Key | 文字模型访问密钥。 |
| 请求超时 | 单次 AI 请求的最长等待时间。 |

#### 多模态模型（AI 辅助接入）

| 参数 | 作用 |
| --- | --- |
| Provider | 多模态模型提供者。 |
| Base URL | 多模态接口基础地址。 |
| 模型 | 用于分析设备截图的模型名称。 |
| API Key | 多模态模型访问密钥。 |
| 请求超时 | 单次多模态请求的最长等待时间。 |
| 思考模式 | Provider 支持时启用模型思考能力。 |

系统状态显示 `stub` 时，应分别检查对应能力的 Provider，而不是仅检查是否填写了模型名称。常见原因包括：

- Provider 仍设置为 `stub` 或模板模式。
- 用户设置被操作系统环境变量覆盖。
- 保存了服务配置但尚未重启程序。
- Base URL、模型或 API Key 不完整。
- OCR 已配置，但 AI 多模态未配置，或反之。

### 5.5 SpecLabOS 平台

| 参数 | 作用 |
| --- | --- |
| 平台 Provider | 选择真实平台或本地模拟模式。 |
| 平台地址 | SpecLabOS 服务基础地址。 |
| 平台 API Key | 平台接口认证密钥。 |
| DataHub Key | SmartDataHub 数据上传认证密钥。 |
| 请求超时 | 平台请求最长等待时间。 |

未配置平台地址或使用模拟 Provider 时，本地设备接入、工作流设计和本地运行仍可使用，但平台登录、模板同步和数据上传等能力不可用或仅进行模拟。

### 5.6 高级网络与消息服务

高级配置包括：

- 是否启用 RabbitMQ 远程任务。
- RabbitMQ 主机、端口、用户名和密码。
- Edge API 主机和端口。
- UDP 主机、端口和超时。

这些参数主要用于平台化执行、远程任务监听和边缘通信。仅进行本地工作流验证时，可暂不启用 RabbitMQ。

### 5.7 输入控件交互规则

系统设置和数据采集页面使用统一输入控件：

- 下拉框具有清晰可见的下拉箭头。
- 数字输入框右侧显示放大的上下调节箭头。
- 鼠标仅悬停在数字输入框上时，滚轮不会修改数值，而是继续滚动页面。
- 必须先点击数字输入框使其获得焦点，滚轮才会调整当前数值。
- 下拉框不会因为鼠标悬停滚轮而误切换选项。

## 6. 设备接入与校准

设备接入页面用于描述“目标软件窗口中，哪个区域是什么，以及动作执行前如何确认当前界面没有错位”。

### 6.1 新建设备

创建设备时建议填写稳定、可读的设备 ID 和名称。设备 ID 将用于：

- 锚点配置目录。
- 工作流设备绑定。
- AI 辅助接入上下文。
- 运行日志和平台识别。

设备 ID 保存后不建议频繁修改。

### 6.2 扫描和选择窗口

1. 先打开目标仪器软件，并进入需要校准的页面。
2. 在 SmartAccess 中点击窗口扫描。
3. 从窗口列表中选择目标窗口。
4. 刷新或获取当前窗口截图。

窗口列表区域已扩大，左侧主要用于展示可选窗口和已保存设备，便于从多个窗口中选择目标。

如果目标软件使用管理员权限运行，SmartAccess 也可能需要使用相同权限启动，否则真实自动化可能无法点击或输入。

### 6.3 视图管理

视图表示同一设备软件的不同页面或状态，例如：

- `main`：主页面。
- `sample`：样品信息页面。
- `result`：结果页面。
- `dialog`：弹窗状态。

视图选择和操作位于画布顶部，包括：

- `新增视图`：基于当前截图创建新视图。
- `保存当前视图`：保存当前视图截图和锚点配置。
- `删除视图`：删除当前非主视图。

`main` 视图为基础视图，不允许删除。

切换视图前建议先保存当前视图，避免未保存的锚点修改丢失。

### 6.4 画布操作

校准画布支持以下交互：

- 在非锚点区域按住鼠标左键拖拽：平移视图。
- 在锚点框内部拖拽：移动锚点。
- 拖动锚点框四角：调整锚点大小。
- `Ctrl + 鼠标滚轮`：缩放截图。
- 截图边缘可继续拖动到画布窗口中间，方便编辑靠近截图边缘的区域。
- 调整窗口大小或缩放后，平移边界会动态更新。

空白区域拖拽默认用于移动视图，不再默认绘制框选橡皮线。添加锚点时，应先进入对应的锚点绘制操作。

### 6.5 锚点配置

锚点表当前主要字段为：

| 字段 | 说明 |
| --- | --- |
| 锚点 ID | 工作流引用该区域时使用的唯一标识。 |
| 目标区域 | 动作实际点击、输入或观察的区域。 |
| 执行前校验 | 动作执行前是否检查当前界面状态。 |
| 校验区域 | 用于图像或文字一致判断的截图区域。 |
| 操作 | 编辑、删除等锚点操作。 |

锚点中不再配置具体动作，也不再配置人工确认。原因如下：

- 同一个锚点可以被不同工作流动作复用。
- 动作参数属于工作流步骤，而不是设备定位信息。
- 人工确认属于具体流程的安全策略，应在工作流步骤中通过 `执行前确认` 配置。

### 6.6 执行前校验

执行前校验用于防止窗口选错、页面切换错误、弹窗遮挡或坐标错位导致误点击。

支持以下模式：

| 模式 | 判断方式 | 适用场景 |
| --- | --- | --- |
| 无 | 不执行额外校验。 | 界面稳定且风险较低。 |
| 图像一致 | 比较参考区域和当前区域的图像相似度。 | 按钮、图标、固定布局或无可靠文字的区域。 |
| 文字一致 | OCR 识别参考区域和当前区域，并比较规范化后的文字。 | 状态文字、标题、字段标签等。 |
| 图像 + 文字 | 图像和文字两项都通过才允许执行。 | 高风险按钮或必须严格确认页面状态的场景。 |

启用执行前校验后，校验区域默认与目标区域一致。只有在目标区域不适合判断状态时，才需要绘制自定义校验区域。

例如点击“开始测试”按钮时，可采用：

- 目标区域：按钮本身。
- 校验区域：默认与按钮区域一致。
- 校验方式：图像一致，确认按钮当前外观与校准截图一致。

如果按钮外观变化较多，但附近存在稳定标题，可改为：

- 目标区域：按钮。
- 自定义校验区域：页面标题。
- 校验方式：文字一致或图像 + 文字。

### 6.7 图像一致的判断方式

图像一致校验会在动作执行前：

1. 获取当前目标窗口截图。
2. 从校准参考截图中裁剪锚点校验区域。
3. 从当前截图中裁剪同一相对区域。
4. 使用图像相似度算法计算分数。
5. 将分数与锚点保存的阈值比较。
6. 达到阈值才允许执行动作。

本地视觉实现使用 OpenCV 等图像处理能力；API 视觉场景会对图像进行裁剪后完成相似度和 OCR 判断。

默认图像相似度阈值为 `0.8`。阈值越高越严格：

- 界面固定、误操作风险高：可适当提高。
- 存在字体抗锯齿、主题、光标或轻微渲染变化：可适当降低。
- 不建议为了让校验通过而持续降低阈值，应优先选择更稳定的校验区域。

### 6.8 文字一致的判断方式

文字一致校验不是简单判断“是否包含某段文字”，而是比较校准参考区域与运行时当前区域的 OCR 结果。

运行时会：

1. OCR 识别校准参考区域。
2. OCR 识别动作执行前的当前区域。
3. 对两侧文字执行 Unicode NFKC 规范化。
4. 清理空白字符并忽略大小写差异。
5. 要求规范化后的结果完全一致。

因此，如果校准时区域文字为“开始测试”，运行时识别为“开始测试”，校验通过；如果运行时页面变成“停止测试”或其他文字，则校验失败，动作不会执行。

选择文字校验区域时应避免包含：

- 时间、日期或动态计数。
- 随机编号和实时状态值。
- 闪烁光标。
- 经常变化的提示信息。
- 过多无关文字。

### 6.9 校验预览

保存设备前，可点击 `校验预览` 检查当前视图中的执行前校验。

预览可帮助确认：

- 校验区域是否正确。
- 当前图像相似度分数是否达到阈值。
- OCR 参考文字和当前文字是否一致。
- 是否需要调整阈值或更换校验区域。

如果预览失败，应先刷新当前视图截图，确认窗口未被遮挡，并检查 OCR Provider 是否可用。

### 6.10 AI 辅助接入

AI 辅助接入使用多模态模型分析当前设备截图，并生成锚点配置建议。

当前 AI 提示词已适配新的锚点模型：

- 锚点负责目标区域和执行前校验，不再绑定具体动作。
- 校验区域默认与目标区域一致。
- AI 可根据界面风险建议图像、文字或组合校验。
- 图像阈值使用当前系统设置中的默认值。

AI 生成结果属于草稿，保存前仍需人工检查锚点位置、命名和校验方式。

### 6.11 保存设备配置

设备配置通常保存到：

```text
workspace\anchors\{设备ID}\anchors.yaml
```

重新保存已有设备时，应注意系统当前的默认图像相似度阈值会同步应用到该设备所有视图的执行前图像校验配置。

## 7. 工作流设计

工作流设计用于描述“按什么顺序，对哪个设备视图和锚点执行什么动作”。

### 7.1 新建工作流

创建工作流时，应设置：

- 工作流 ID。
- 工作流名称。
- 绑定设备。
- 必要的描述或业务信息。

工作流 ID 用于保存目录和运行记录，不建议保存后频繁修改。

### 7.2 步骤工具栏

步骤工具栏当前提供：

- `添加动作`：在列表末尾添加动作步骤。
- `插入动作`：在当前选中位置插入动作步骤。
- `插入等待`：在当前选中位置插入等待步骤。

人工确认不是单独的一行步骤，OCR 条件也不再附加到所有动作上：

- 需要人工确认时，勾选该动作行的 `执行前确认`。
- 需要等待文字条件时，显式添加一个 `OCR识别` 动作。

### 7.3 支持的动作类型

当前仅保留实际需要的动作：

| 动作 | 作用 |
| --- | --- |
| 单击 | 点击锚点目标区域的中心或定义位置。 |
| 输入 | 在锚点对应输入区域中输入内容。 |
| 快捷键 | 发送组合键。 |
| 回车 | 向当前目标发送 Enter。 |
| OCR识别 | 轮询识别锚点区域，直到满足文字条件或超时。 |
| 等待 | 固定等待指定时间。 |

当前不提供拖拽、滚动、悬停等扩展动作，避免增加不必要的配置复杂度。

### 7.4 步骤表字段

工作流步骤表当前包括：

| 字段 | 说明 |
| --- | --- |
| 步骤 | 自动生成的步骤名称，例如 `step_1`。 |
| 动作 | 当前步骤的动作类型。 |
| 视图 | 动作执行时预期设备所在视图。 |
| 锚点 | 动作引用的设备锚点。 |
| 参数摘要 | 当前动作专属参数的简要说明。 |
| 动作后等待 | 动作成功后额外等待的秒数。 |
| 执行前确认 | 是否在执行该动作前请求人工确认。 |
| 操作 | 上移、下移、删除等操作。 |

步骤列会在插入、删除、上移或下移后自动重新编号：

```text
step_1
step_2
step_3
```

步骤名称按当前实际顺序生成，避免插入步骤后编号混乱。

### 7.5 动作参数配置弹窗

不同动作需要的参数不同，因此动作专属参数不再全部堆叠在表格中，而是统一放入 `动作参数配置` 弹窗。

打开方式：

- 点击对应的参数配置入口。
- 双击 `参数摘要` 单元格。

参数摘要只展示关键信息，详细配置在弹窗中完成，可明显减少表格横向拥挤。

### 7.6 各动作参数

#### 单击

主要配置视图、锚点和动作后等待。实际点击区域来自锚点的目标区域，动作执行前先执行锚点配置的校验。

#### 输入

输入支持：

- `自由输入`：输入固定值、运行时提供值或模板内容。
- `递增式`：根据格式模板生成递增编号。

递增式可使用设备 ID、作者、日期、计数器、工作流 ID、会话等占位信息。配置后应通过摘要或预览确认最终格式。

#### 快捷键

配置需要发送的组合键。应使用目标软件可识别的标准按键组合，并避免与 SmartAccess 自身快捷键冲突。

#### 回车

发送 Enter，一般用于提交输入框内容或确认目标软件中的默认操作。

#### OCR识别

OCR 动作可配置：

| 参数 | 说明 |
| --- | --- |
| 匹配方式 | 期望文字与识别结果的匹配规则。 |
| 期望文本 | OCR 识别需要满足的目标文字。 |
| 识别超时 | 最长等待时间，默认 `10 秒`。 |
| 轮询间隔 | 两次 OCR 识别之间的等待时间，默认 `0.5 秒`。 |
| 最低置信度 | 过滤低置信度识别结果。 |

OCR 动作适合等待业务状态变化，例如：

```text
单击“开始”
  -> OCR识别等待“运行完成”
  -> 单击“导出”
```

#### 等待

配置固定等待时长。等待动作本身的等待时间与普通动作的 `动作后等待` 含义不同：

- 等待动作：该步骤的核心行为就是等待。
- 动作后等待：单击、输入、OCR 成功等动作完成后额外暂停。

### 7.7 动作后等待

所有步骤均可独立配置 `动作后等待`，默认值为 `1.0 秒`。

建议：

- 界面响应快且稳定：可设置较短时间或 `0`。
- 页面切换、弹窗打开、数据加载：适当增加等待。
- 能通过 OCR 判断完成状态时，优先使用 OCR 动作，不要仅依赖很长的固定等待。

### 7.8 执行前确认

`执行前确认` 是工作流步骤的安全控制，不属于锚点配置，也不是独立步骤。

运行到已勾选的动作时：

1. 工作流在动作执行前暂停。
2. SmartAccess 主窗口恢复并激活。
3. 人工确认弹窗以应用模态方式显示在最前面。
4. 用户可以直接查看步骤信息并点击确认。
5. 确认后才继续执行动作。

人工确认只激活 SmartAccess 和置顶确认框，不使用任务栏闪烁提醒。在用户完成确认前，流程保持等待状态。

建议为以下步骤启用：

- 启动真实实验。
- 删除、覆盖或提交不可逆数据。
- 打开高风险设备功能。
- 需要人工检查样品或仪器状态的步骤。

### 7.9 锚点校验与人工确认的关系

一个动作执行前的推荐顺序为：

```text
到达工作流步骤
  -> 获取当前设备窗口
  -> 执行锚点的图像/文字校验
  -> 校验通过后，如已勾选则等待人工确认
  -> 人工确认后执行动作
  -> 执行动作后等待
```

两者作用不同：

- 执行前校验：确认“当前窗口、页面和位置是否与校准状态一致”。
- 人工确认：在自动校验通过后，确认“现在是否允许执行这个业务动作”。

高风险动作可以同时启用人工确认和锚点执行前校验。

### 7.10 检查、保存与 AI 生成

保存前应检查：

- 工作流引用的设备是否存在。
- 每个步骤的视图和锚点是否有效。
- 动作参数是否完整。
- OCR 超时和轮询间隔是否合理。
- 执行前确认是否配置在真正需要的步骤上。
- 动作后等待是否过长或过短。

工作流草稿通常保存到：

```text
workspace\workflows\{工作流ID}\draft.yaml
```

AI 工作流生成使用系统设置中的文字模型。当前提示词已同步适配：

- 当前支持的六种动作。
- 参数弹窗对应的动作专属参数。
- 默认动作后等待。
- 默认 OCR 超时和轮询间隔。
- 执行前确认字段。
- 当前步骤和锚点数据结构。

AI 生成结果应作为草稿使用，运行前必须人工检查步骤顺序、设备锚点和安全确认设置。

## 8. 运行监控

运行监控用于启动工作流、查看实时状态和处理人工确认。

### 8.1 启动工作流

1. 在顶部工作流下拉框选择已保存工作流。
2. 确认目标仪器软件已经打开且窗口状态正确。
3. 点击启动运行。
4. 根据需要填写运行时输入参数。
5. 观察当前会话、步骤状态和日志。

工作流下拉框使用统一的可见箭头控件，便于确认当前选择。

### 8.2 停止工作流

运行过程中可点击停止。停止请求会由运行引擎处理，日志中会记录停止状态。涉及真实仪器时，停止软件工作流不一定等于停止仪器硬件动作，应根据仪器安全规程操作。

### 8.3 人工确认弹窗

遇到 `执行前确认` 步骤时：

- SmartAccess 主窗口会恢复并激活。
- 确认弹窗显示在最前面。
- 弹窗为应用模态，避免隐藏在其他 SmartAccess 页面后方。
- 用户点击确认后流程继续。

如果运行一直停留在确认状态，应切换到 SmartAccess 检查确认框，而不是重复启动同一工作流。

### 8.4 执行前校验日志

执行前校验信息会写入运行日志，包括：

- 步骤 ID、视图和锚点。
- 校验模式。
- 当前尝试次数和重试信息。
- 图像相似度分数与阈值。
- OCR 参考文字和当前文字。
- 图像校验、文字校验的分别结果。
- 最终通过或失败原因。

如果校验失败，动作不会执行。排查时应先查看日志中的实际分数和 OCR 文字，再决定调整截图、区域、OCR 服务或阈值。

### 8.5 运行会话与产物

运行产物通常保存到：

```text
workspace\runs\{session_id}\
```

可包含：

- 会话状态。
- 步骤事件。
- OCR 识别事实。
- 执行前校验详情。
- 运行截图或其他审计产物。
- trace 和错误信息。

### 8.6 系统状态

运行监控中的系统状态会按实际能力显示，例如：

```text
OCR: paddlex
AI 文字: deepseek
AI 多模态: qwen
```

具体名称取决于当前配置，不应固定理解为某个 Provider。三个状态含义分别为：

- `OCR`：文字识别服务。
- `AI 文字`：工作流文本生成服务。
- `AI 多模态`：设备截图分析和锚点生成服务。

## 9. 模板/平台

模板/平台页面用于管理工作流模板和平台同步状态。

主要能力包括：

- 根据设备和工作流创建本地模板。
- 查看模板状态和版本信息。
- 发布模板到平台。
- 从平台同步模板。
- 删除本地或平台模板记录。

页面顶部的设备、工作流和状态选择器使用统一的可见下拉箭头，并禁止鼠标悬停滚轮误切换选项。

本地模板通常保存到：

```text
workspace\templates\{模板ID}\{版本}\workflow.yaml
```

是否能够连接真实平台，取决于系统设置中的平台 Provider、平台地址、认证信息和网络状态。未配置真实平台时，可继续使用本地设备、工作流和运行能力。

## 10. 数据采集

数据采集页面用于监听仪器软件产生的本地文件或目录，并将数据可靠上传到 SmartDataHub。

页面顶部提供：

- `保存配置`：保存采集器和监听器配置。
- `启动采集`：启动本地监听和上传队列。
- `停止采集`：停止当前采集运行。

### 10.1 采集器参数

| 参数 | 说明 |
| --- | --- |
| 采集器 ID | 当前采集实例的唯一标识。 |
| 站点 | 数据来源站点或实验室标识。 |
| 请求超时 | 单次上传请求的最长等待时间。 |
| 失败重试间隔 | 上传失败后的等待时间。 |
| 最大重试次数 | 单条数据允许的最大自动重试次数。 |
| 启动时扫描当前已存在的数据 | 启动采集时扫描监听目录中已有文件。 |
| 强制重新上传已扫描过的数据 | 忽略已扫描标记，重新处理历史数据。 |

启用强制重新上传前，应确认平台是否允许重复数据，避免产生重复记录。

### 10.2 监听器管理

监听器区域支持：

- `添加监听器`。
- `编辑`当前监听器。
- `删除`当前监听器。

每个监听器可配置：

| 参数 | 说明 |
| --- | --- |
| 名称 | 监听器唯一名称。 |
| 类型 | 文件或目录资产监听方式。 |
| 监听目录 | 仪器数据产生目录，通过目录选择器指定。 |
| 匹配模式 | 要采集的文件模式，多个模式使用英文逗号分隔，例如 `*.csv, *.json`。 |
| 数据类型 | 上传时标记的业务数据类型。 |
| 递归监听子目录 | 是否扫描监听目录下的所有子目录。 |
| 稳定等待 | 文件出现后等待其写入稳定的时间。 |
| 采集文件后续修改 | 已发现文件再次更新时是否重新采集。 |
| 更新防抖 | 文件频繁修改时，合并更新事件的等待时间。 |

稳定等待非常重要。仪器软件可能先创建文件，再持续写入内容；等待时间过短可能上传未写完的文件。

### 10.3 启动采集

推荐顺序：

1. 在系统设置中配置平台地址、DataHub Key 和网络超时。
2. 在数据采集页填写采集器参数。
3. 添加至少一个有效监听器。
4. 保存配置。
5. 点击 `启动采集`。
6. 观察监听器状态、队列和失败记录。

数据采集配置通常保存到：

```text
workspace\data_collection\collector.yaml
```

可靠上传队列通常保存在：

```text
workspace\data_collection\collector_queue.db
```

### 10.4 失败记录处理

上传达到最大重试次数后，会进入终止失败记录区域。可选择失败记录并点击 `手动重试`，将其重新加入上传队列。

手动重试前应先排查：

- SmartDataHub 地址是否可访问。
- DataHub Key 是否有效。
- 文件是否仍然存在且可读取。
- 数据类型是否受平台支持。
- 网络超时是否过短。
- 是否因为重复数据被平台拒绝。

## 11. 运行概览

运行概览用于快速查看当前工作区状态，通常包括：

- 已保存设备数量。
- 工作流数量。
- 模板数量。
- 最近运行会话。
- 运行状态或异常摘要。

如果看不到刚保存的设备或工作流，优先检查：

1. 当前系统设置中的工作区是否正确。
2. `SMARTACCESS_WORKSPACE_DIR` 是否被操作系统环境变量覆盖。
3. 是否已在对应页面点击保存。
4. 是否在切换工作区后仍查看旧目录。

## 12. 工作区目录与主要产物

典型工作区结构如下：

```text
workspace\
├─ app_state\
│  └─ application_settings.json
├─ anchors\
│  └─ {设备ID}\
│     └─ anchors.yaml
├─ workflows\
│  └─ {工作流ID}\
│     └─ draft.yaml
├─ templates\
│  └─ {模板ID}\
│     └─ {版本}\
│        └─ workflow.yaml
├─ runs\
│  └─ {session_id}\
└─ data_collection\
   ├─ collector.yaml
   └─ collector_queue.db
```

说明：

- 不建议在 SmartAccess 运行期间手动编辑这些文件。
- 手工修改 YAML 或 JSON 前应先备份。
- 删除工作区文件会永久移除对应本地配置或历史记录。
- 切换工作区不会自动迁移上述数据。

## 13. 常见问题与排查

### 13.1 扫描不到目标窗口

- 确认目标程序已经打开且不是最小化到托盘。
- 尝试使用相同权限运行 SmartAccess 和目标程序。
- 刷新窗口列表。
- 避免选择临时弹窗、隐藏窗口或无标题窗口。

### 13.2 截图或坐标不正确

- 确认运行时窗口尺寸、缩放比例和校准时一致。
- 确认目标窗口没有被其他窗口遮挡。
- 重新刷新截图并保存视图。
- 使用画布平移和缩放检查边缘锚点。
- 为关键动作启用执行前图像或文字校验。

### 13.3 图像校验一直失败

- 查看运行日志中的实际相似度分数和阈值。
- 检查校验区域是否包含光标、动画、时间或动态数据。
- 重新截取清晰、稳定的参考图像。
- 优先调整校验区域，不要只降低阈值。
- 确认系统默认阈值修改后，已有设备是否已重新保存。

### 13.4 文字校验一直失败

- 查看日志中的参考文字和当前文字。
- 确认 OCR Provider 可用。
- 缩小区域，只保留稳定文字。
- 避免动态数值、日期和状态变化文本。
- 检查截图是否清晰，文字是否被遮挡。

### 13.5 OCR 动作等待时间过长

- 检查期望文本和匹配方式。
- 将默认超时和轮询间隔设置为合理值。
- 确认最低置信度没有设置过高。
- 确认选择了正确视图和锚点。
- 观察日志中的每次 OCR 结果。

### 13.6 人工确认弹窗看不到

- SmartAccess 会主动恢复并激活主窗口，检查当前桌面前台窗口。
- 不要重复启动工作流，当前流程可能正在等待确认。
- 检查是否有其他系统级置顶窗口遮挡 SmartAccess。
- 确认步骤确实勾选了 `执行前确认`。

### 13.7 系统状态显示 stub

- 分别检查自动化、OCR、AI 文字、AI 多模态和平台 Provider。
- 确认保存配置后是否需要重启。
- 检查操作系统环境变量是否覆盖用户设置。
- 检查 Base URL、模型和认证信息是否完整。
- 不要把 OCR Provider 和 AI 多模态 Provider 混为同一项配置。

### 13.8 修改系统默认阈值后旧锚点未变化

系统默认值不会在修改瞬间批量改写所有设备文件。应打开已有设备配置并重新保存，保存时会将当前系统默认图像相似度同步到该设备所有视图的执行前图像校验。

### 13.9 数字输入框被滚轮误改

当前控件只有获得点击焦点后才响应滚轮。未点击时滚轮应滚动外层页面。如果仍发生误改，应确认使用的是最新版本，并检查是否有其他旧版自定义数字控件未更新。

### 13.10 数据采集没有发现文件

- 确认监听目录存在且可访问。
- 检查匹配模式是否正确。
- 确认是否需要递归监听子目录。
- 检查采集服务是否已启动。
- 确认文件已达到稳定等待时间。
- 已存在文件需要启用启动时扫描。

## 14. 安全使用建议

- 首次运行工作流时，应有人值守并使用测试数据。
- 对启动实验、删除、覆盖、提交等动作启用执行前确认。
- 对关键按钮启用图像、文字或组合校验。
- 校验失败时不要盲目降低阈值，应先检查页面和区域。
- OCR 动作应设置合理超时，避免工作流无限等待。
- 不要在文档、日志截图或代码提交中公开 API Key 和密码。
- 真实仪器运行前，应确认软件自动化停止不会影响硬件安全。
- 发布 AI 生成的设备或工作流配置前，必须进行人工审查。
- 数据采集启用强制重新上传前，应确认平台的重复数据策略。

## 15. 快速验收清单

### 15.1 系统设置

- [ ] 工作区可以通过目录选择器修改。
- [ ] 配置保存到 `workspace\app_state\application_settings.json`。
- [ ] 默认动作后等待为 `1.0 秒`。
- [ ] 默认 OCR 超时为 `10 秒`。
- [ ] OCR 轮询间隔可以配置。
- [ ] 默认图像相似度为 `0.8`。
- [ ] 数字输入框未点击时滚轮不会误改数值。
- [ ] 下拉框和数字输入框箭头显示正常。

### 15.2 设备接入

- [ ] 视图选择和视图操作位于画布顶部。
- [ ] `main` 视图不能删除。
- [ ] 空白区域拖拽可以平移截图。
- [ ] 截图边缘可以移动到画布中间。
- [ ] 新增执行前校验时，校验区域默认与目标区域一致。
- [ ] 图像、文字、图像 + 文字校验均可配置。
- [ ] 校验预览可显示相似度或 OCR 结果。
- [ ] 修改系统图像阈值后，重新保存设备会更新已有锚点阈值。

### 15.3 工作流设计

- [ ] 步骤列正常显示。
- [ ] 插入、移动、删除步骤后自动重新编号。
- [ ] 参数摘要列可双击打开配置弹窗。
- [ ] 表格只显示通用字段，动作参数在弹窗配置。
- [ ] 动作后等待独立显示，默认 `1.0 秒`。
- [ ] OCR 超时默认 `10 秒`，轮询间隔可配置。
- [ ] 执行前确认使用复选框，而不是独立步骤。
- [ ] AI 生成结果使用当前动作和默认参数模型。

### 15.4 运行监控

- [ ] 人工确认时 SmartAccess 主窗口会被激活。
- [ ] 确认弹窗显示在最前面且可以点击。
- [ ] 执行前校验结果写入运行日志。
- [ ] 图像分数、阈值和 OCR 文字可在日志中查看。
- [ ] 系统状态分别显示 OCR、AI 文字和 AI 多模态 Provider。

### 15.5 数据采集与平台

- [ ] 模板/平台下拉箭头正常显示且不会滚轮误切换。
- [ ] 数据采集数字输入控件与系统设置样式一致。
- [ ] 监听器可以添加、编辑和删除。
- [ ] 启动采集后可以发现匹配文件。
- [ ] 终止失败记录可以手动重新入队。

完成以上检查后，即可继续进行真实设备的完整流程验证、模板发布和平台联调。
