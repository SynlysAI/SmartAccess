# SmartAccess 完整功能使用说明

> 适用目录：`E:\github_project\SmartAccess\dist`  
> 当前目标：先跑通本地工作流编排与执行闭环，暂不配置发布、云端同步、远程任务监听等平台化操作。

## 1. 产品定位

SmartAccess 是面向 Windows 科学仪器上位机软件的非侵入式接入与执行工具。它不改造原始仪器软件，也不要求仪器提供 SDK/API，而是通过以下能力把人工操作沉淀为可复用工作流：

- 窗口扫描：识别当前桌面可操作窗口。
- 截图校准：截取目标窗口画面，作为锚点标注基准。
- ROI/锚点配置：框选按钮、输入框、状态文本等界面区域。
- OCR 观测：对指定观察区域进行文字识别，用于判断步骤是否达成预期。
- UI 自动化执行：通过真实 Win32 自动化完成点击、输入、快捷键、回车等动作。
- 工作流编排：把一组动作步骤保存为标准 `workflow.yaml`。
- 运行审计：记录每次运行的步骤日志、OCR 事实、截图路径和 trace。

当前建议优先采用“锚点配置 -> 工作流设计 -> 本地运行监控 -> 复盘调整”的闭环方式验证仪器流程。

## 2. 目录与启动方式

### 2.1 发布目录

当前发布目录为：

```text
E:\github_project\SmartAccess\dist\SmartAccess
```

主要文件/目录说明：

- `run_desktop.exe`：SmartAccess 桌面端启动程序。
- `.env`：当前发布包的运行配置文件。
- `resource/`：图标等资源文件。
- `workspace/`：运行时工作区，保存设备锚点、工作流、运行记录、模板记录等。如果目录不存在，程序会按配置自动创建。
- 其他大量 `.dll`、`.pyd`、Python 包目录：Nuitka 打包后的运行依赖，不建议手动改动。

### 2.2 启动步骤

1. 进入发布目录：

   ```powershell
   cd E:\github_project\SmartAccess\dist\SmartAccess
   ```

2. 启动桌面端：

   ```powershell
   .\run_desktop.exe
   ```

3. 如果没有配置 `SPECLABOS_BASE_URL`，程序会跳过统一门户登录，直接进入本地桌面工作台。
4. 如果配置了 `SPECLABOS_BASE_URL`，启动时会显示 SpecLabOS 登录框，并进行执行端注册校验。

## 3. 推荐本地跑通流程

当前阶段建议不要先做发布、云端同步和远程控制，先按以下顺序跑通本地闭环：

```text
打开目标仪器软件
  -> SmartAccess 扫描窗口
  -> 截图并框选锚点/观察区
  -> 保存设备锚点
  -> 创建工作流并绑定锚点
  -> 检查工作流
  -> 运行监控中启动工作流
  -> 查看日志和运行记录
  -> 回到锚点/工作流页微调
```

推荐先选一个简单流程验证，例如：

- 点击某个按钮；
- 在输入框输入固定文本；
- 按回车；
- 用 OCR 检查结果区域出现目标文字。

## 4. 主界面导航

SmartAccess 主界面左侧提供 5 个主要页面：

1. `设备接入与校准`：窗口扫描、截图、视图管理、ROI 框选、锚点保存、OCR 预览。
2. `工作流设计`：新建/编辑工作流、添加步骤、绑定锚点、配置 OCR 条件、AI 生成、标准化检查、保存。
3. `运行监控`：选择工作流、启动/停止执行、查看运行会话和步骤日志。
4. `模板/平台`：本地模板记录、模板发布、云端同步、删除等平台相关入口。
5. `运行概览`：查看本地工作区中的锚点、工作流、模板、运行记录、异常等统计信息。

右侧 `系统状态` 面板会根据当前页面显示上下文信息。顶部的 `☰` 可折叠左侧导航，`ⓘ` 可折叠右侧状态面板。

## 5. 设备接入与校准

该页面负责把目标软件界面转成可复用的锚点配置。锚点是后续工作流步骤执行的基础。

### 5.1 新建设备配置

1. 点击 `设备接入与校准`。
2. 点击右上角 `新建`。
3. 在 `设备 ID` 输入框中填写唯一设备标识。
4. 建议使用四段式命名，便于后续维护：

   ```text
   体系-实验室-产品型号-设备编号
   ```

   示例：

   ```text
   氟基-2236实验室-元能极片电阻仪-01
   ```

5. 在 `窗口标题关键字` 中填写目标软件窗口标题的一部分，用于筛选窗口。

### 5.2 扫描窗口

1. 先打开目标仪器上位机软件，并确保目标窗口可见、未最小化。
2. 回到 SmartAccess，点击 `扫描`。
3. 在窗口列表中选择目标窗口。
4. 如果列表太多，可以通过 `窗口标题关键字` 缩小范围。

注意事项：

- 目标窗口不要被其他窗口遮挡。
- 尽量固定目标窗口大小和位置，否则已标注 ROI 可能需要重新校准。
- 如果目标软件需要管理员权限，SmartAccess 也可能需要以相同权限运行，避免自动化点击受限。

### 5.3 截图与视图管理

1. 选择目标窗口后点击 `截图`。
2. 中间画布会展示目标窗口截图。
3. 如果一个仪器流程涉及多个页面/弹窗，可使用 `新增视图` 增加视图。
4. 调整某个视图截图或锚点后，点击 `保存当前视图`。
5. 不再需要的视图可点击 `删除视图`。

视图用于区分同一软件的不同界面状态，例如：

- `main`：主界面；
- `settings`：参数设置页；
- `result`：结果页；
- `dialog`：弹窗确认页。

### 5.4 添加锚点

1. 点击 `添加锚点`。
2. 输入锚点 ID。
3. 在截图画布中拖动/调整 ROI 到目标位置。
4. 在右侧锚点表中设置动作类型和观察方式。

锚点 ID 建议用英文小写和下划线，例如：

```text
start_button
sample_name_input
result_text
confirm_button
```

锚点表字段说明：

- `锚点 ID`：后续工作流引用的稳定名称。
- `动作区域`：真正执行点击/输入/快捷键的 ROI。
- `动作`：默认动作类型，可选单击、输入、快捷键、回车、OCR识别。
- `OCR`：是否为该动作额外启用观察区域。
- `观察区域`：用于 OCR 判断的 ROI，通常框选状态文本、结果值、日志区域。
- `确认`：运行到该锚点动作时是否需要人工确认。
- `操作`：删除该行锚点。

### 5.5 动作与观察区关系

SmartAccess 当前采用简化模型：

- 一个锚点必须有一个动作区域。
- 一个锚点可以选择性绑定一个 OCR 观察区域。
- 工作流步骤执行动作后，如果配置 OCR 条件，则轮询观察区域文本并判断是否匹配。
- 如果没有 OCR 条件，则按默认等待时间继续下一步。

常见配置方式：

- 按钮点击：动作区域框选按钮本身，通常不需要 OCR。
- 输入框输入：动作区域框选输入框，可按需配置结果提示/日志区域为观察区。
- 状态检查：动作类型选择 OCR识别，动作区域/观察区域框选状态文本区域。
- 弹窗确认：动作区域框选确认按钮，并勾选人工确认，避免误操作。

### 5.6 OCR 预览

1. 先完成截图。
2. 在锚点表中选择或配置需要 OCR 的观察区域。
3. 点击 `OCR预览`。
4. 弹窗会展示当前识别文本。

如果 OCR 结果为空或明显不准确，优先检查：

- 观察 ROI 是否框准；
- 目标窗口是否被遮挡；
- 字体是否过小或对比度不足；
- OCR 服务地址是否可访问；
- 当前 `.env` 中的 OCR 模式是否正确。

### 5.7 AI 辅助接入

页面上有 `AI辅助接入` 入口，用于根据截图和文字描述生成锚点草稿。

当前发布包中，多模态识别图片的 AI 辅助接入暂时因为模型原因不可用；现阶段不要依赖该入口完成设备接入。建议先使用人工截图、手动框选 ROI、OCR 预览校准的方式跑通流程。

### 5.8 保存与复用设备

1. 确认设备 ID、窗口匹配规则、视图、锚点表均已配置。
2. 点击 `保存`。
3. 保存后会出现在左侧 `已保存设备` 列表。
4. 双击已保存设备可重新载入编辑。

保存后的锚点配置通常位于工作区：

```text
workspace\anchors\{设备ID}\anchors.yaml
```

## 6. 工作流设计

该页面负责把锚点串成可执行流程。

### 6.1 新建工作流

1. 点击 `工作流设计`。
2. 点击 `新建`。
3. 填写基础信息：
   - `工作流 ID`：唯一标识，例如 `wf_sample_measurement`。
   - `设备`：选择已保存的设备锚点集。
   - `作者`：填写当前编排人。
   - `状态`：通常本地调试阶段保持草稿状态。
4. 从步骤区域开始添加动作。

### 6.2 步骤类型

步骤表支持以下动作：

- `单击 click`：移动到锚点中心并点击。
- `输入 type`：聚焦锚点并输入文本。
- `快捷键 hotkey`：在锚点或当前窗口上下文发送快捷键。
- `回车 press_enter`：发送回车。
- `OCR识别 ocr`：只读取目标观察区域文本，不执行点击输入。
- `等待 wait`：等待固定秒数，或等待 OCR 条件满足。

工作流页面提供快捷按钮：

- `添加动作`：在末尾添加普通动作步骤。
- `插入动作`：在当前选中位置插入普通动作步骤。
- `插入等待`：插入固定等待步骤。
- `等待OCR`：插入基于 OCR 条件的等待步骤。
- `人工确认`：插入需要人工确认的步骤。

### 6.3 步骤字段说明

步骤表主要字段如下：

- `步骤 ID`：步骤唯一名称，建议保持简短可读。
- `动作`：选择本步骤执行的动作类型。
- `视图`：选择该步骤对应的设备视图。
- `锚点`：选择要操作或观察的锚点。
- `值`：输入动作的文本、快捷键内容等。
- `输入模式`：支持自由输入和递增式输入。
- `等待`：本步骤执行后的等待秒数。
- `OCR 条件`：配置文本匹配规则、期望文本、超时、置信度等。
- `确认`：运行时是否需要人工确认。
- `操作`：调整步骤顺序或删除步骤。

### 6.4 输入模式

`输入` 步骤支持两种输入模式：

- `自由输入`：每次运行输入固定值。
- `递增式`：根据规则自动生成运行值，适合样品编号、实验编号等。

默认递增规则示例：

```text
{device_id}-{author}-{date}-{counter:03d}
```

可用占位符包括：

- `{device_id}`：当前设备 ID。
- `{author}`：作者。
- `{date}`：日期。
- `{counter}`：计数器。
- `{workflow_id}`：工作流 ID。
- `{workflow_name}`：工作流名称。
- `{session}`：运行会话 ID。

### 6.5 OCR 条件

OCR 条件用于判断步骤是否完成，支持：

- `无`：不做 OCR 判断。
- `包含`：识别文本包含期望文本。
- `等于`：识别文本与期望文本完全一致。
- `正则`：使用正则表达式匹配。
- `非空`：识别结果非空即可通过。

其他可配置项：

- `期望文本`：用于匹配的文本；多个候选值可用 `|` 分隔。
- `超时秒数`：在指定时间内轮询 OCR，超时则失败。
- `最低置信度`：要求 OCR 结果达到指定置信度。
- `Aa`：忽略大小写。
- `NFKC`：归一化空白和全半角字符。

### 6.6 检查与保存

1. 配置完成后点击 `检查`。
2. 查看右侧信息区中的检查结果。
3. 如果提示缺失锚点、动作不合法、OCR 条件不完整等，按提示修正。
4. 检查通过后点击 `保存`。

保存后的工作流通常位于：

```text
workspace\workflows\{工作流ID}\draft.yaml
```

### 6.7 AI 生成工作流

`AI生成` 用于根据文字描述和已保存锚点生成工作流草稿。当前 `.env` 中已配置文字 LLM，因此该入口理论上可用于本地工作流草稿生成。

使用建议：

- 先手动保存准确的设备锚点。
- Prompt 中明确操作顺序、输入内容、预期 OCR 文本。
- AI 生成后必须人工检查每一步绑定的视图、锚点、动作和值。
- 生成结果只作为草稿，不建议不检查就直接运行。

## 7. 运行监控

运行监控用于执行本地工作流并查看运行过程。

### 7.1 启动运行

1. 点击 `运行监控`。
2. 在 `工作流` 下拉框中选择已保存工作流。
3. 确认目标仪器软件窗口已经打开，并处于锚点截图对应的界面状态。
4. 点击 `开始`。
5. 运行时不要移动目标窗口，不要遮挡目标窗口，也不要抢占鼠标键盘。

### 7.2 停止运行

- 点击 `停止` 可请求停止当前运行。
- 如果目标仪器软件已经进入危险或不可逆状态，应优先按实验室安全流程人工接管。

### 7.3 查看运行会话

左侧 `运行会话` 会列出历史运行记录。选中会话后可查看对应日志和状态。

运行记录通常位于：

```text
workspace\runs\{session_id}\
```

常见产物包括：

- 步骤日志；
- OCR 识别事实；
- 截图或裁剪图路径；
- `run_trace.jsonl`；
- 失败原因和异常详情。

### 7.4 日志判断

运行日志会标识任务开始和结束边界，并输出关键上下文，例如设备 ID、作者、工作流、session 等。

重点关注：

- 是否成功选中目标窗口；
- 每个步骤是否找到对应锚点；
- OCR 实际文本是否符合预期；
- 超时、置信度不足、人工确认取消等失败原因；
- 输入步骤是否写入了预期内容。

## 8. 模板/平台

该页面面向平台化发布和同步，当前阶段暂不建议启用。

### 8.1 页面能力

页面提供以下入口：

- 选择设备；
- 选择工作流；
- 填写模板 ID；
- 填写模板版本；
- `发布`：把标准化工作流发布为模板；
- `同步云端`：从平台回拉模板/状态；
- `删除`：删除本地模板记录；
- 按模板、版本、设备、错误搜索；
- 按状态过滤模板记录。

### 8.2 当前使用建议

当前 `.env` 中平台提供者为 `stub`，且 `SPECLABOS_BASE_URL` 为空，因此不会真正连接 SpecLabOS 平台。

在本地编排流程未稳定前，建议：

- 不点击 `发布`；
- 不配置 `SPECLABOS_BASE_URL`；
- 不开启远程任务监听；
- 先在本地完成设备锚点、工作流、运行监控闭环。

待本地流程稳定后，再切换平台真实配置并验证模板发布、云端同步、远程调度和心跳上报。

## 9. 运行概览

`运行概览` 用于查看本地工作区总体状态。

常见统计包括：

- 已保存设备/锚点数量；
- 工作流数量；
- 模板记录数量；
- 运行会话数量；
- 异常/事件数量；
- 平台同步相关状态。

使用方式：

1. 点击 `运行概览`。
2. 点击 `刷新`。
3. 根据统计结果确认本地资产是否正常生成。

如果看不到刚保存的设备或工作流，优先检查当前 `.env` 的 `SMARTACCESS_WORKSPACE_DIR` 是否指向预期工作区。

## 10. 常见本地调试建议

### 10.1 窗口与截图

- 保持目标窗口大小固定。
- 不要在运行过程中移动目标窗口。
- 避免系统缩放变化影响坐标。
- 目标窗口不要被 SmartAccess 或其他窗口遮挡。

### 10.2 锚点命名

- 使用英文小写、数字、下划线。
- 名称表达业务含义，不要只叫 `button1`、`input2`。
- 同一个设备内锚点 ID 保持唯一。

### 10.3 OCR 条件

- 优先框选稳定文本，不要框太大。
- 对变化文本使用 `包含` 或 `正则`。
- 对只需要确认出现内容的区域使用 `非空`。
- 超时时间不要过短，涉及仪器响应建议从 5-30 秒开始调。

### 10.4 工作流安全

- 首次运行时建议每个关键步骤勾选人工确认。
- 先用无风险动作验证点击和输入位置。
- 涉及真实仪器运动、加热、加压、通电等动作前，必须人工确认。
- 工作流稳定后再逐步减少人工确认。

## 11. 当前 `.env` 配置情况

配置文件路径：

```text
E:\github_project\SmartAccess\dist\SmartAccess\.env
```

### 11.1 基础配置

| 配置项 | 当前状态 | 说明 |
| --- | --- | --- |
| `SMARTACCESS_LOG_LEVEL` | `INFO` | 使用普通信息日志级别。 |
| `SMARTACCESS_WORKSPACE_DIR` | `workspace` | 使用发布目录下的相对工作区。 |
| `SMARTACCESS_AUTOMATION_PROVIDER` | `real` | 启用真实 Win32 UI 自动化，适合本地窗口操作。 |
| `SMARTACCESS_DEVICE_ID` | `pc-lab-tiepi` | 当前执行端/本机标识已配置。 |

### 11.2 OCR 配置

| 配置项 | 当前状态 | 说明 |
| --- | --- | --- |
| `SMARTACCESS_OCR_MODE` | `paddlex` | 使用远程 OCR/API 模式。 |
| `SMARTACCESS_OCR_API_URL` | `http://47.121.24.228:7060` | 当前 OCR 服务地址已配置。 |

说明：当前 OCR 不是本地 `local` 模式，而是通过远程 OCR 服务进行识别。如果 OCR 预览或运行等待 OCR 失败，需要优先确认该地址在当前网络环境可访问。

### 11.3 文字 LLM 配置

| 配置项 | 当前状态 | 说明 |
| --- | --- | --- |
| `SMARTACCESS_AI_TEXT_PROVIDER` | `deepseek` | 文字 AI 已切到 DeepSeek provider。 |
| `SMARTACCESS_AI_TEXT_BASE_URL` | `https://api.deepseek.com/v1` | 文字模型接口地址已配置。 |
| `SMARTACCESS_AI_TEXT_MODEL` | `deepseek-v4-pro` | 当前文字模型名已配置。 |
| `SMARTACCESS_AI_TEXT_API_KEY` | 已配置，文档中不明文展示 | 可用于工作流 `AI生成` 草稿。 |
| `SMARTACCESS_AI_TEXT_TIMEOUT_SECONDS` | `60` | 文字 AI 请求超时时间为 60 秒。 |

说明：文字 LLM 主要用于“根据文字描述生成工作流草稿”。生成结果仍需人工检查、保存和本地运行验证。

### 11.4 多模态 AI 配置

| 配置项 | 当前状态 | 说明 |
| --- | --- | --- |
| `SMARTACCESS_AI_VISION_PROVIDER` | `template` | 当前多模态 provider 为模板/占位模式。 |
| `SMARTACCESS_AI_VISION_BASE_URL` | 已填写 | 由于 provider 为 `template`，当前不会真正启用。 |
| `SMARTACCESS_AI_VISION_MODEL` | `GPT-5.4` | 当前模型名已填写，但暂不作为可用能力。 |
| `SMARTACCESS_AI_VISION_API_KEY` | 已配置，文档中不明文展示 | 由于 provider 为 `template`，当前不应视为已启用。 |
| `SMARTACCESS_AI_VISION_TIMEOUT_SECONDS` | `60` | 多模态请求超时时间配置值。 |

重要说明：当前多模态识别图片的 AI 辅助接入暂时因为模型原因不可用。设备接入阶段请先使用本地手动校准方式：扫描窗口、截图、框选 ROI、OCR 预览、保存锚点。

### 11.5 平台配置

| 配置项 | 当前状态 | 说明 |
| --- | --- | --- |
| `SMARTACCESS_PLATFORM_PROVIDER` | `stub` | 平台客户端为本地占位模式，不真正发布/同步到 SpecLabOS。 |
| `SPECLABOS_BASE_URL` | 空 | 未配置平台地址，启动时会跳过统一门户登录。 |
| `SPECLABOS_API_KEY` | 已填写占位值，文档中不明文展示 | 当前因平台 provider/base_url 状态，不作为真实发布凭据使用。 |
| `SPECLABOS_TIMEOUT_SECONDS` | `20` | 平台请求超时时间配置值。 |

说明：当前平台发布、云端同步、心跳上报等操作均不作为本阶段目标。请先不要配置真实发布流程。

### 11.6 远程任务监听配置

| 配置项 | 当前状态 | 说明 |
| --- | --- | --- |
| `SMARTACCESS_RABBITMQ_ENABLED` | `false` | 远程任务监听已禁用。 |
| `SMARTACCESS_RABBITMQ_HOST` | 已填写 | 当前不会连接，因为 enabled=false。 |
| `SMARTACCESS_RABBITMQ_PORT` | `5672` | 当前不会连接，因为 enabled=false。 |
| `SMARTACCESS_RABBITMQ_USERNAME` | 已填写 | 当前不会使用。 |
| `SMARTACCESS_RABBITMQ_PASSWORD` | 已填写，文档中不明文展示 | 当前不会使用。 |

说明：当前不会从 RabbitMQ 接收远程任务，本地桌面端只需要手动点击 `运行监控 -> 开始` 执行工作流。

## 12. 当前阶段结论

当前发布包适合做以下事情：

- 本地启动 SmartAccess 桌面端；
- 扫描并校准本机已打开的仪器软件窗口；
- 手动创建视图、ROI 和锚点；
- 使用远程 OCR 服务做 OCR 预览和运行期文本判断；
- 创建、检查、保存本地工作流；
- 通过运行监控手动启动本地工作流；
- 查看本地运行日志和 trace。

当前不建议做以下事情：

- 配置真实 SpecLabOS 发布；
- 使用模板页发布或同步云端；
- 开启 RabbitMQ 远程任务监听；
- 依赖多模态图片识别自动生成锚点；
- 未经人工确认直接运行高风险仪器动作。

阶段目标应保持为：先把本地工作流编排流程跑通，再进入平台发布、远程调度和多模态 AI 辅助接入的联调阶段。
