# SmartAccess UI 改进 - 修改说明

## 完成日期
2026年6月8日

## 修改内容总览

根据用户反馈，本次更新实施了三项重要改进：

### 1. ✅ 新增回车键（Enter）动作支持

**需求背景**：
在 step_6 步骤中需要输入文字后按回车键确认，因此需要在仪器能力和动作中增加回车键功能。

**实施方案**：
- 在 `calibration_page.py` 的 `_ALL_ACTIONS` 列表中添加了新的动作类型：
  - `("press_enter", "按回车键", "在输入框中按回车键确认输入")`
- 将 `press_enter` 添加到默认动作集合 `_DEFAULT_ACTIONS` 中
- 在 `workflow_page.py` 的 `_ACTION_NOTES` 中同步添加了该动作的说明

**影响范围**：
- 校准页面：仪器能力配置中新增"按回车键"选项，默认勾选
- 工作流设计页面：步骤编排中可以选择 `press_enter` 动作
- 锚点标定：锚点动作绑定中可以选择"按回车键"

**使用方式**：
用户在配置工作流时，可以在需要的位置添加 `press_enter` 动作：
```yaml
- id: step_6
  action: type
  target: message_input
  value: "ClaudeCode测试"
- id: step_7
  action: press_enter
  target: message_input
```

---

### 2. ✅ AI 生成步骤的手动编辑功能

**需求背景**：
工作流设计中，如果 AI 整理的步骤有问题，需要有可以人工手动修改、校正、确认的入口。

**实施方案**：
将原本只读的步骤显示区域改造为可编辑的表格：

**修改文件**：`workflow_page.py`

**核心功能**：
1. **步骤表格化编辑**：
   - 替换原有的只读文本显示为可编辑的 `QTableWidget`
   - 表格列：步骤 ID、动作、目标、值、上移/下移、删除

2. **新增操作功能**：
   - ✏️ **编辑**：直接在表格单元格中修改步骤 ID、目标、值
   - 🔽 **动作选择**：通过下拉框选择动作类型（click、type、press_enter、hotkey 等）
   - ➕ **添加步骤**：点击"+ 添加步骤"按钮新增步骤
   - 🗑️ **删除步骤**：每行有独立的"删除"按钮
   - ⬆️⬇️ **调整顺序**：使用上移/下移按钮调整步骤执行顺序

3. **数据收集与保存**：
   - 新增 `_collect_steps()` 方法从表格收集步骤数据
   - 修改 `_build_form_workflow()` 方法，将编辑后的步骤保存到工作流
   - 保存后步骤会持久化到 `workflow.yaml`

**用户体验改进**：
- 提示文本更新为："AI 生成的步骤可在此手动修改、调整顺序或删除。确认无误后保存工作流。"
- 所有修改实时反映在表格中，保存后生效
- 支持完整的 CRUD 操作（创建、读取、更新、删除）

---

### 3. ✅ 表格字体和尺寸优化

**需求背景**：
图2（ROI 绑定表格）和图3（锚点标定表格）的字体显示不清晰，需要调整表格的尺寸。

**实施方案**：

#### 3.1 全局样式调整（`_qss.py`）
```css
/* 表格头部 */
QHeaderView::section {
    padding: 8px 10px;        /* 原：6px 8px */
    font-size: 14px;          /* 新增：明确字体大小 */
}

/* 表格内容 */
QTableWidget {
    font-size: 14px;          /* 新增：明确字体大小 */
}

QTableWidget::item {
    padding: 6px 8px;         /* 原：4px 6px，增大内边距 */
}
```

#### 3.2 表格行高调整
在以下文件中为所有表格设置了更大的默认行高：

**`workflow_page.py`**：
- **步骤表格**：行高 36px
- **ROI 绑定表格**：行高 36px
- **输出项表格**：行高 36px

**`calibration_page.py`**：
- **锚点标定表格**：行高 38px（因为包含下拉框，需要更多空间）

**代码实现**：
```python
self._binding_table.verticalHeader().setDefaultSectionSize(36)
self._anchor_table.verticalHeader().setDefaultSectionSize(38)
```

**效果**：
- ✅ 字体从默认 13px 增大到 14px
- ✅ 表格行高增加，文字显示更清晰
- ✅ 单元格内边距增大，视觉更舒适
- ✅ 表头更醒目，高度和字体都得到优化

---

## 技术细节

### 新增的方法（workflow_page.py）

1. **`_populate_steps_table(workflow)`**
   - 从工作流对象填充步骤表格

2. **`_add_step_row()`**
   - 添加新的空白步骤行

3. **`_insert_step_row(step_id, action, target, value)`**
   - 在表格中插入一行步骤数据，包含所有控件

4. **`_delete_step_row(row)`**
   - 删除指定行并重新绑定按钮事件

5. **`_move_step_up(row)` / `_move_step_down(row)`**
   - 向上/向下移动步骤

6. **`_swap_step_rows(row1, row2)`**
   - 交换两行步骤的数据

7. **`_collect_step_row_data(row)`**
   - 收集指定行的步骤数据

8. **`_set_step_row_data(row, data)`**
   - 设置指定行的步骤数据

9. **`_rebind_step_buttons()`**
   - 重新绑定所有按钮的事件处理器（防止闭包问题）

10. **`_collect_steps()`**
    - 收集表格中的所有步骤，转换为 `WorkflowStep` 对象列表

### 数据验证

在 `_collect_steps()` 方法中添加了数据验证：
- 步骤 ID 不能为空
- 动作类型必须选择
- 目标和值允许为空（某些动作不需要）

---

## 兼容性说明

### 向后兼容
- ✅ 所有现有的 `instrument_profile.yaml` 文件无需修改
- ✅ 现有的 `workflow.yaml` 文件可以正常加载和编辑
- ✅ 新增的 `press_enter` 动作是可选的，不影响现有配置

### 数据迁移
不需要数据迁移。现有配置文件中：
- 如果仪器配置中没有 `press_enter` 动作，用户可以手动添加
- 如果步骤中需要按回车，可以手动添加新步骤

---

## 测试建议

### 1. 回车键功能测试
- [ ] 在校准页面的仪器能力中确认"按回车键"选项默认勾选
- [ ] 在锚点动作绑定中确认可以选择"按回车键"
- [ ] 生成 instrument_profile.yaml 后确认 `press_enter` 在 actions 列表中
- [ ] 在工作流设计页面添加 press_enter 步骤并保存

### 2. 步骤编辑功能测试
- [ ] 生成一个新工作流，确认步骤显示在可编辑表格中
- [ ] 修改步骤的 ID、动作、目标、值，确认可以保存
- [ ] 使用上移/下移按钮调整步骤顺序
- [ ] 删除某个步骤，确认删除成功
- [ ] 添加新步骤，确认可以添加并保存
- [ ] 保存后重新加载工作流，确认修改已持久化

### 3. 表格显示测试
- [ ] 确认 ROI 绑定表格字体清晰可读
- [ ] 确认锚点标定表格字体清晰可读
- [ ] 确认步骤表格字体清晰可读
- [ ] 确认表格行高适中，不拥挤
- [ ] 确认下拉框和按钮在单元格内显示正常

---

## 用户操作指南

### 使用回车键功能

1. **配置仪器能力**：
   - 进入"设备接入与校准"页面
   - 在"属性"标签页中，确认"press_enter — 按回车键"已勾选
   - 保存 instrument_profile.yaml

2. **在工作流中使用**：
   - 进入"工作流设计"页面
   - 在步骤表格中添加新步骤或编辑现有步骤
   - 动作下拉框中选择"press_enter · 按回车键"
   - 设置目标为需要按回车的输入框锚点名称

### 手动编辑 AI 生成的步骤

1. **查看步骤**：
   - 生成工作流后，步骤会自动显示在"步骤编排"表格中

2. **修改步骤**：
   - 直接点击单元格修改步骤 ID、目标、值
   - 使用动作下拉框更改动作类型

3. **调整顺序**：
   - 点击"↑"按钮向上移动步骤
   - 点击"↓"按钮向下移动步骤

4. **添加步骤**：
   - 点击"+ 添加步骤"按钮
   - 在新行中填写步骤信息

5. **删除步骤**：
   - 点击对应行的"删除"按钮

6. **保存修改**：
   - 确认所有修改无误后，点击"保存工作流配置"按钮

---

## 后续建议

1. **批量操作**：
   - 考虑添加批量选择和删除步骤的功能
   - 支持复制/粘贴步骤

2. **步骤验证**：
   - 添加步骤的实时验证（如检查目标锚点是否存在）
   - 步骤之间的依赖关系检查

3. **撤销/重做**：
   - 实现步骤编辑的撤销/重做功能
   - 保留编辑历史

4. **拖拽排序**：
   - 支持鼠标拖拽来调整步骤顺序
   - 替代或补充现有的上移/下移按钮

5. **步骤模板**：
   - 提供常用步骤的模板快速插入
   - 如"点击并等待"、"输入并回车"等组合操作

---

## 文件变更清单

### 修改的文件
1. `src/smartaccess/desktop/pages/workflow_page.py`
   - 新增步骤编辑表格和相关方法
   - 修改工作流显示和保存逻辑

2. `src/smartaccess/desktop/pages/calibration_page.py`
   - 添加 press_enter 动作
   - 调整表格行高

3. `src/smartaccess/desktop/shell/_qss.py`
   - 优化表格样式
   - 增大字体和内边距

### 未修改的文件
- `src/smartaccess/shared/contracts/workflow.py` - 数据模型无需改动
- `src/smartaccess/runtime/` - 运行时逻辑无需改动

---

## 总结

本次更新成功实现了用户提出的三个需求：
1. ✅ 回车键功能已完整集成到仪器能力和工作流系统中
2. ✅ AI 生成的步骤现在完全可编辑，支持增删改查和排序
3. ✅ 表格显示已优化，字体更大更清晰，行高更合适

所有修改均保持了向后兼容性，不影响现有配置文件和工作流的使用。
